<%= form_for(@baker) do |f| %>
  <% if @baker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@baker.errors.count, "error") %> prohibited this baker from being saved:</h2>

      <ul>
      <% @baker.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="field">
    <%= f.label :baked_at %><br>
    <%= f.datetime_select :baked_at %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
